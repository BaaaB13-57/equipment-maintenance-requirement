# Required in production (use a MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER/equipment?retryWrites=true&w=majority

NODE_ENV=production
# Set true only for the first deployment if you need the initial demo accounts.
SEED_DATABASE=false
# The hosting service normally provides PORT automatically.
PORT=3000
